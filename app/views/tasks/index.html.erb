<h1>Tasks</h1>

<div id="tasks" class="d-flex flex-row align-items-center mx-auto flex-wrap">
  <% @tasks.each do |task| %>
    <%= render task %>
  <% end %>
</div>

<%= link_to "New task", new_task_path, class: "btn btn-primary", style: "margin-top: 10px;" %>
