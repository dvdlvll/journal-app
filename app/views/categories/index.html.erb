<h1>Categories</h1>

<div id="categories" class="d-flex flex-row flex-wrap">
  <% @categories.each do |category| %>
    <%= render category %>
  <% end %>
</div>

<%= link_to "New Category", new_category_path, class: "btn btn-primary", style: "margin-top: 10px;"  %>
